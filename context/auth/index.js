import {
  REQUEST_LOGIN,
  LOGIN_SUCCESS,
  LOGOUT,
  LOGIN_ERROR,
  SET_USER,
  SET_TOKEN,
} from "./constants";

const authConstants = {
  REQUEST_LOGIN,
  LOGIN_SUCCESS,
  LOGOUT,
  LOGIN_ERROR,
  SET_USER,
  SET_TOKEN,
};

export default authConstants;
